<!--
Thanks for the PR! Two things below are required and checked automatically
before this can be merged — a PR check will fail (and block the merge
button) if either is missing. This is what drives the automatic CHANGELOG.md
entry, git tag, and GitHub Release created when this merges, so both need
to describe the change as it should appear to someone reading the release
notes, not implementation detail.
-->

## What kind of change is this?

Check exactly one, using [Semantic Versioning](https://semver.org/):

- [ ] Major — breaking change
- [ ] Minor — new backward-compatible feature
- [ ] Patch — fix, no new capability

## Changelog description

<!--
This becomes the CHANGELOG.md entry for this release, verbatim. Write it
the way you'd want a user to read it — plain description, one bullet per
line if there's more than one notable thing. No need to add a version
header or date, that's added automatically.
-->
